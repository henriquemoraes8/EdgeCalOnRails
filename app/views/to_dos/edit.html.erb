<h1>Edit To-do</h1>

<%= form_for(@todo, :url => {:action => 'update', :id => @todo.id}) do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', {:action => 'index'} %>
