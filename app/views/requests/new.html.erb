<h1>New Request</h1>

<%= form_for(@request_map, :url => {:action => 'create', :id => @request_map.id}) do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', {:action => 'index'} %>